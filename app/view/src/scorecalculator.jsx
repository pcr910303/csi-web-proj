const React = require("react");

import withStyles from '@material-ui/core/styles/withStyles';

const styles = theme => ({

});

function ScoreCalculator(props) {
  const { classes } = props;

  return (
    <div>
    </div>
  );
}

export default withStyles(styles)(ScoreCalculator);
